import { CommonModule } from '@angular/common';
import { Component } from '@angular/core';

@Component({
    selector: 'framed-table-column',
    standalone: true,
    imports: [CommonModule],
    template: ` <p>table-column works!</p> `
})
export class FramedTableColumnComponent {}
